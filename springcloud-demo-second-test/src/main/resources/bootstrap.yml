#eureka
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
spring:
  application:
    name: test2
  cloud:
    config:
      profile: test
      discovery:
        enabled: true
        service-id: CONFIG
      name: second
ribbon:
  eureka:
    enabled: true
logging:
  level:
    org:
      springframework:
        cloud:
          openfeign: debug

#eureka
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
spring:
  application:
    name: test
  cloud:
    config:
      profile: dev
      discovery:
        enabled: true
        service-id: CONFIG
      name: test

